# This is a Databricks asset bundle definition for bundle_recursos.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: bundle
  uuid: 3c37d4fb-588f-490e-8e46-5f8e350049d4

include:
  - resources/*.yml
  - resources/*/*.yml


# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    default: true
    workspace:
      host: https://adb-7405617078931419.19.azuredatabricks.net/
      root_path: /Workspace/Projeto//.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: wk_dev_edu
      schema: bronze
  prod:
  
    workspace:
      host: https://adb-7405609494406006.6.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/eduardoedu08_gmail.com#ext#@eduardoedu08gmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Projeto//.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: wk_prd_edu
      schema: bronze
    permissions:
      - user_name: eduardoedu08_gmail.com#ext#@eduardoedu08gmail.onmicrosoft.com
        level: CAN_MANAGE
